//! LSP manager for lifecycle management

use crate::lsp::LspClient;
use crate::types::LspError;

pub struct LspManager {
    // TODO: Implement LSP manager
}

impl LspManager {
    pub fn new() -> Result<Self, LspError> {
        Ok(Self {})
    }
}
